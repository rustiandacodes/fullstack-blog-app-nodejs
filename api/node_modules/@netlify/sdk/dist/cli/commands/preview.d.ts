import type { ContentEnginePublicAPI } from "content-engine";
interface PreviewOpts {
    /** Run the preview in local dev mode */
    dev?: boolean;
    /** Run the preview in local dev mode */
    d?: boolean;
    /** Preview the GraphQL server for your Netlify Connect plugin" */
    connector?: boolean;
    /** Enable UI Preview. If this is not set it will be enabled by default */
    ui?: boolean;
}
export declare const preview: (opts: PreviewOpts) => Promise<{
    engine?: false | ContentEnginePublicAPI;
}>;
export {};
